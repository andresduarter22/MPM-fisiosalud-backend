{
    "$jsonSchema": {
        "bsonType": "object",
        "required": [
            "contact_name",
            "contact_phone_number"
        ],
        "properties": {
            "contact_name": {
                "bsonType": "string",
                "description": "Name of the contact on list."
            },
            "contact_phone_number": {
                "bsonType": "string",
                "description": "Phone number of the contact."
            },
            "contact_email": {
                "bsonType": "string",
                "description": "Email address number of the contact."
            },
            "additional_info": {
                "bsonType": "string",
                "description": "Extra information of the contact."
            }
        }
    }
}