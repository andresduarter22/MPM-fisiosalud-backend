{
    "$jsonSchema": {
        "bsonType": "object",
        "required": [
            "_id",
            "staff_name",
            "staff_phone_number",
            "staff_email",
            "staff_role"
        ],
        "properties": {
            "_id": {
                "bsonType": "string",
                "description": "Staff identification."
            },
            "staff_name": {
                "bsonType": "string",
                "description": "Name of the worker."
            },
            "staff_phone_number": {
                "bsonType": "string",
                "description": "Contact phone number of the worker."
            },
            "staff_email": {
                "bsonType": "string",
                "description": "Contact email address of the worker."
            },
            "staff_role": {
                "bsonType": "string",
                "description": "Role of the worker inside the system."
            }
        }
    }
}