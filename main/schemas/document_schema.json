{
    "$jsonSchema": {
        "bsonType": "object",
        "required": [
            "path",
            "description"
        ],
        "properties": {
            "patient": {
                "bsonType": "object",
                "description": "Information of the patient that is related with the document."
            },
            "path": {
                "bsonType": "string",
                "description": "Path of the docuemnt inside the server."
            },
            "description": {
                "bsonType": "string",
                "description": "Document description."
            }
        }
    }
}